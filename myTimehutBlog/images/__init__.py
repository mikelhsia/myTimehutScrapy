# Now we need to tell Django where our application configuration resides. Edit the __init__.py file
# located inside the images application directory and add the following line to it:

default_app_config = 'images.apps.ImagesConfig'
